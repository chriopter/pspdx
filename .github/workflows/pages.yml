name: Pages

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Never cancel a deploy that is already running; queue behind it instead.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/configure-pages@v5

      - name: Generate site
        run: |
          mkdir -p _site
          python3 catalog/build.py _site/catalog.json
          cat > _site/index.html <<'HTML'
          <!doctype html>
          <meta charset="utf-8">
          <title>PSPDX</title>
          <h1>PSPDX</h1>
          <p>PSP Download Index. The client fetches <a href="catalog.json">catalog.json</a>.</p>
          <p>Everything else -- the client, the catalog entries, the format --
          is at <a href="https://github.com/chriopter/pspdx">github.com/chriopter/pspdx</a>.</p>
          HTML

      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
